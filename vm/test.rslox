var edad = 15;
var hijo_directivo = false;

if (edad < 18 and !hijo_directivo) {
  print "lo siento, aun no tienes la edad suficiente.";
} else if (edad < 25 or hijo_directivo) {
  print "bienvenido al sistema";
} else {
  print "No cumples con el rango de edad requerido";
}
